{
  "name": "codex-client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve-heroku": "vite --host 0.0.0.0 --port $PORT preview"

  },
  "devDependencies": {
    "vite": "^4.0.0"
  }
  
}
